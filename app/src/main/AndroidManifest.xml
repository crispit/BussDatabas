<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:sharedUserId="my.app"
    package="com.example.fredrikhansson.komigennuraa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestList"
            android:label="Skickade felrapporter">
        </activity>
        <activity android:name=".SymptomList"
            android:label="Val av symptom">

        </activity>
        <activity android:name=".UpdateReport"
            android:label="Komplettera eller radera felrapport">

        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET"/>


</manifest>